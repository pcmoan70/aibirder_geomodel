# Core
numpy
pandas
geopandas
pyarrow
shapely
h3
scikit-learn
tqdm

# Deep learning
torch

# Visualization
matplotlib
cartopy
Pillow

# Analysis (optional — only for scripts/)
scipy

# Data collection (optional — only for building datasets)
earthengine-api
python-dotenv

# Model export (optional — only for convert.py)
onnx
onnxruntime
onnxconverter-common
onnxscript
onnx2tf
tensorflow

# Hyperparameter tuning (optional — only for --autotune)
optuna